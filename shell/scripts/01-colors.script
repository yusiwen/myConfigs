#!/bin/sh

if [ "$(uname)" != 'Darwin' ]; then
  # eval "$(dircolors -b "$HOME/myConfigs/shell/LS_COLORS/LS_COLORS")"
  eval "$(dircolors -b "$HOME/myConfigs/shell/dircolors-solarized/dircolors.ansi-universal")"
fi

