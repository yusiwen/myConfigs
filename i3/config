# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
font pango:Sans 9

# By default, window focus follows your mouse movements. However, if you have a
# setup where your mouse usually is in your way (like a touchpad on your laptop
# which you do not want to disable completely), you might want to disable focus
# follows mouse and control focus only by using your keyboard. The mouse will
# still be useful inside the currently active window (for example to click on
# links in your browser window).
focus_follows_mouse no

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# colors

#class                  border  backgr.text
#client.focused          #121212 #101010 #9e8a8e
#client.focused          #121212 #101010 #cfcfcf
#client.focused_inactive #101010 #191919 #999999
#client.unfocused        #101010 #191919 #999999
#client.urgent           #cd989a #cd989a #2e3436

#client.background       #1d1d1d

# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec urxvtc
bindsym $mod+Shift+Return exec roxterm

# kill focused window
bindsym $mod+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace 1:Main
bindsym $mod+2 workspace 2:Web
bindsym $mod+3 workspace 3:Mail
bindsym $mod+4 workspace 4:Book
bindsym $mod+5 workspace 5:Python
bindsym $mod+6 workspace 6:Eclipse
bindsym $mod+7 workspace 7:Office
bindsym $mod+8 workspace 8:VBox
bindsym $mod+9 workspace 9:Chat
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1:Main
bindsym $mod+Shift+2 move container to workspace 2:Web
bindsym $mod+Shift+3 move container to workspace 3:Mail
bindsym $mod+Shift+4 move container to workspace 4:Book
bindsym $mod+Shift+5 move container to workspace 5:Python
bindsym $mod+Shift+6 move container to workspace 6:Eclipse
bindsym $mod+Shift+7 move container to workspace 7:Office
bindsym $mod+Shift+8 move container to workspace 8:Vbox
bindsym $mod+Shift+9 move container to workspace 9:Chat
bindsym $mod+Shift+0 move container to workspace 10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Create Log out, Reboot, Poweroff bindings
mode "Exit >> (L)ogout, (X)Reboot, (P)oweroff  (C)Reload (R)Restart" {
        bindsym Shift+x exec dbus-send --system --print-reply --dest="org.freedesktop.ConsoleKit" /org/freedesktop/ConsoleKit/Manager org.freedesktop.ConsoleKit.Manager.Restart
        bindsym Shift+l exit
        bindsym Shift+p exec dbus-send --system --print-reply --dest="org.freedesktop.ConsoleKit" /org/freedesktop/ConsoleKit/Manager org.freedesktop.ConsoleKit.Manager.Stop
        bindsym Shift+c reload
        bindsym Shift+r restart

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+Shift+q mode "Exit >> (L)ogout, (X)Reboot, (P)oweroff  (C)Reload (R)Restart"

# bind key for i3lock
bindsym $mod+Control+l exec i3lock -i /home/yusiwen/.wallpaper/lock.png
# bind key for pidgin
bindsym $mod+p exec pidgin
# bind key for ec.py (Eclipse Environment Chooser)
bindsym $mod+Shift+p exec /home/yusiwen/bin/ec.py
# bind key for Google Chrome Browser
bindsym $mod+z exec google-chrome
# bind key for Thunderbird mail client
bindsym $mod+Shift+x exec geary
# bind key for Alpine, run in a new urxvt window
bindsym $mod+x exec urxvtc -e mutt
# bind key for weechat-curses, run in a new urxvt window
bindsym $mod+Shift+o exec urxvtc -e weechat-curses
# bind key for ncmpcpp, run in a new urxvt window
bindsym $mod+o exec urxvtc -e ncmpcpp
# bind key for MidnightCommander, run in a new urxvt window
bindsym $mod+m exec urxvtc -geometry 160x42 -e mc -c
# bind key for thunar
bindsym $mod+Shift+m exec thunar
# bind key for Catfish
bindsym $mod+c exec catfish


# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3status --config ~/.i3/i3status.config
#        colors {
#                background #101010
#                statusline #999999
#                focused_workspace #825969 #191919
#                active_workspace #825969 #101010
#                focused_workspace #cfcfcf #191919
#                active_workspace #cfcfcf #101010
#                inactive_workspace #9a878f #191919
#                urgent_workspace #2e3436 #9a878f
#        }
}

exec --no-startup-id xbacklight -set 75
exec --no-startup-id xrdb -merge /home/yusiwen/.Xresources
exec --no-startup-id source /home/yusiwen/keyring.sh
exec --no-startup-id xcompmgr -n
exec --no-startup-id dunst
exec feh --bg-scale /home/yusiwen/.wallpaper/wallpaper.jpg
exec python /home/yusiwen/goagent-3.0.5/local/goagent-gtk.py
exec --no-startup-id gnome-settings-daemon
exec --no-startup-id /usr/lib/policykit-1-gnome/polkit-gnome-authentication-agent-1
exec --no-startup-id thunar --daemon
exec --no-startup-id remmina -i
exec --no-startup-id nm-applet
exec --no-startup-id blueman-applet
exec --no-startup-id indicator-virtualbox
exec --no-startup-id urxvtd -q -o -f
exec --no-startup-id volti
#exec --no-startup-id goldendict
exec --no-startup-id mpd ~/.mpd/mpd.conf
exec --no-startup-id /home/yusiwen/mTouch.py>/dev/null
exec --no-startup-id parcellite
exec --no-startup-id orage

# Assign applications to workspace
assign [class="Google-chrome"] → 2:Web
assign [class="Geary"] → 3:Mail
assign [class="URxvt" title="mutt"] → 3:Mail
assign [class="Calibre-gui"] → 4:Book
assign [class="Wiznote"] → 4:Book
assign [class="Eric4.py"] → 5:Python
assign [class="Komodo"] → 5:Python
assign [class="Eclipse"] → 6:Eclipse
assign [class="Java" title="Eclipse"] → 6:Eclipse
assign [class="Wps"] → 7:Office
assign [class="Et"] → 7:Office
assign [class="VirtualBox"] → 8:VBox
assign [class="URxvt" title="weechat"] → 9:Chat

for_window [class="Eclipse" title="Workspace Launcher "] floating enable
for_window [class="Java" title="Eclipse "] floating enable
for_window [class="Gimp"] floating enable
for_window [class="Gpick"] floating enable
for_window [class="Volti"] floating enable
for_window [class="Volti-mixer"] floating enable
for_window [class="KeePass2"] floating enable
for_window [class="Goldendict"] floating enable
# for_window [class="Ubuntu-tweak"] floating enable
for_window [class="URxvt" title="^ncmpcpp"] floating enable
for_window [class="URxvt" title="^mc"] floating enable
for_window [class="Komodo edit" title="About Komodo"] floating enable
for_window [class="Komodo edit" title="Preferences"] floating enable
for_window [class="Komodo edit" title="Stackato Interface"] floating enable
for_window [class="Komodo edit" title="Komodo Edit Help"] floating enable
for_window [class="Komodo edit" title="Software Update"] floating enable
for_window [class="Font-manager"] floating enable
for_window [class="Grub-customizer"] floating enable
for_window [class="Thunar" title="文件操作进度"] floating enable
for_window [class="Flareget"] floating enable
for_window [class="Catfish"] floating enable
for_window [class="Pidgin"] floating enable
